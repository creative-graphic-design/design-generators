# layout-dm

Diffusers-style LayoutDM package for the released Rico25 and PubLayNet checkpoints.

The package keeps public layouts in normalized center `xywh` format and imports shared output, bbox, labels, and discrete helpers from `laygen.common`.
