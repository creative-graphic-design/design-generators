[project]
name = "layout-generation-common"
version = "0.1.0"
description = "Shared schema and utilities for layout generation models."
requires-python = ">=3.11"
dependencies = [
    "torch>=2.0.0",
]

[build-system]
requires = ["uv_build>=0.9.0,<0.10.0"]
build-backend = "uv_build"
