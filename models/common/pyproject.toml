[project]
name = "layout-generation-common"
version = "0.1.0"
description = "Shared layout generation schemas and validation helpers."
requires-python = ">=3.11"
dependencies = [
    "torch>=2.0.0",
]

[build-system]
requires = ["hatchling"]
build-backend = "hatchling.build"
