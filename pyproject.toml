[project]
name = "design-generators"
version = "0.1.0"
description = "Add your description here"
readme = "README.md"
requires-python = ">=3.11"
dependencies = [
    "layout-corrector",
    "layout-dm",
    "laygen",
    "transformers[torch,vision]>=5.0.0",
]

[project.optional-dependencies]
diffusion = [
    "diffusers[torch]>=0.36.0",
]
download = [
    "gdown>=6.1.0",
]
llm-agent = [
    "pydantic-ai>=2.7.0",
]
training = [
    "lightning>=2.0.0",
]

[dependency-groups]
dev = [
    "pre-commit>=4.6.0",
    "pytest>=6.0.0",
    "ruff>=0.1.5",
    "ty>=0.0.10",
    "vulture>=2.16",
]

[tool.uv.workspace]
members = ["lib/*", "models/*"]

[tool.uv.sources]
layout-dm = { workspace = true }
layout-corrector = { workspace = true }
laygen = { workspace = true }
posgen = { workspace = true }

[tool.ruff]
extend-exclude = ["vendor"]

[tool.ruff.lint]
extend-select = ["ANN", "D"]

[tool.ruff.lint.per-file-ignores]
"lib/*/tests/*.py" = ["ANN", "D"]
"lib/*/tests/**/*.py" = ["ANN", "D"]
"models/*/scripts/**/*.py" = ["ANN", "D"]
"models/*/tests/*.py" = ["ANN", "D"]
"models/*/tests/**/*.py" = ["ANN", "D"]
"scripts/**/*.py" = ["ANN", "D"]
"tests/*.py" = ["ANN", "D"]
"tests/**/*.py" = ["ANN", "D"]

[tool.ruff.lint.pydocstyle]
convention = "google"

[tool.ty.rules]
all = "error"
invalid-argument-type = "ignore"
invalid-assignment = "ignore"
invalid-method-override = "ignore"
invalid-return-type = "ignore"
missing-override-decorator = "ignore"
missing-type-argument = "ignore"
unresolved-attribute = "ignore"
unresolved-import = "ignore"
unsupported-base = "ignore"

[tool.ty.src]
exclude = ["vendor/"]

[tool.pytest.ini_options]
addopts = ["--import-mode=importlib"]
testpaths = ["tests", "lib", "models"]
markers = [
    "integration: tests that require external downloads or heavyweight runtime setup",
    "vendor_parity: requires original model weights and vendor dependencies",
]

[tool.vulture]
min_confidence = 70
sort_by_size = true
paths = ["main.py", "lib", "models"]
verbose = false
