# Existing weak top-type cast targets that predate the cast checker.
# These casts sit at dynamic data boundaries such as JSON, dataset rows,
# vendor/config payloads, and framework component dictionaries. Do not add
# entries for new code; replace new cast targets with precise local types.
