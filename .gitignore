# Python-generated files
__pycache__/
*.py[oc]
build/
dist/
site/
docs/api/
docs/index.md
mkdocs.generated.yml
wheels/
*.egg-info/

# Virtual environments
.venv

# Local caches and generated parity fixtures
.cache/
models/*/tests/vendor_parity/fixtures/
.pytest_cache/

# Training and evaluation output that tools write to the working directory
# instead of `.cache/`. Anchored to the repository root: a nested `tmp/` or
# `outputs/` can be a real source directory, a top-level one cannot.
/wandb/
/lightning_logs/
/outputs/
/multirun/
/.hydra/
/tmp/
/coverage-reports/
